/** Chat-specific enums and constants. */

export enum MessageType {
  TEXT = "text",
  IMAGE = "image",
  SYSTEM = "system",
}

export enum ConversationType {
  HOUSEHOLD = "household",
  DIRECT = "direct",
}
