/** Constants for apps and integrations. */

export enum IntegrationStatus {
  CONNECTED = "connected",
  DISCONNECTED = "disconnected",
  ERROR = "error",
}

export enum IntegrationCategory {
  FINANCE = "finance",
  SMART_HOME = "smart_home",
  PRODUCTIVITY = "productivity",
  CALENDAR = "calendar",
}
